# 旧版文档归档

这里保存项目早期围绕旧控制台、Windows 工作流、MoveIt/OpenRAVE 和拆分式通信设计的文档。

当前主线文档已收敛到：

```text
ROS 2 Jazzy + Pinocchio + ProxSuite + MuJoCo + Rerun
```

归档文档仅用于追溯历史决策，不作为当前运行说明。新增功能应更新 `docs/` 下的现行文档，避免恢复旧架构术语。
