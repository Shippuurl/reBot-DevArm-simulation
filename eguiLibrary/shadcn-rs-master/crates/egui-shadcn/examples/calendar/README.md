# Calendar Example

Matches the reference `calendar-demo.tsx` from `reference/shadcn-ui/apps/v4/registry/new-york-v4/examples/calendar-demo.tsx`:

- `mode="single"`
- `selected` defaults to today
- `captionLayout="dropdown"`
- container style equivalent to `rounded-md border shadow-sm`

## Run

```bash
cargo run --example calendar
```

